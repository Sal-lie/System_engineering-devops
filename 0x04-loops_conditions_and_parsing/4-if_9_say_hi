#!/usr/bin/env bash
# This script uses a while loop and an if statement to display "Best School" 10 times, adding "Hi" on the 9th iteration
i=1
while [ "$i" -le 10 ]; do
    echo "Best School"
    if [ "$i" -eq 9 ]; then
        echo "Hi"
    fi
    ((i++))
done
